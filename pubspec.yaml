name: easy_dynamic_theme
description: Automatically get your OS defined Theme (Dynamic), force your prefered one (Light / Dark) and persist your choice in the device.
version: 2.3.1
homepage: "https://github.com/rlazom/easy_dynamic_theme"

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

  #  Mock library for Dart
  mockito: ^5.4.0